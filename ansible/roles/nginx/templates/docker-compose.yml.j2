version: "3.9"

services:
  nginx:
    image: nginx:1.25-alpine
    container_name: dearlavion-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/dearlavion-app.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - dearlavion-net

networks:
  dearlavion-net:
    external: true
