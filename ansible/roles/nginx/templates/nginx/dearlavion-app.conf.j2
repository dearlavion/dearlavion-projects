# ======================
# HTTPS server
# ======================
server {
    listen 443 ssl;
    server_name {{ server_names | join(' ') }};

    ssl_certificate /etc/letsencrypt/live/{{ server_names[0] }}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/{{ server_names[0] }}/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    # ACME HTTP-01 challenge for Certbot
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # ======================
    # Frontend
    # ======================
    location / {
        proxy_pass http://dearlavion-web-ui:{{ web_ui_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ======================
    # Auth Service
    # ======================
    location /auth/ {
        proxy_pass http://dearlavion-authentication-service:{{ auth_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ======================
    # Core Service
    # ======================
    location /core/ {
        proxy_pass http://dearlavion-core-service:{{ core_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ======================
# HTTP redirect to HTTPS
# ======================
server {
    listen 80;
    server_name {{ server_names | join(' ') }};

    # ACME challenge for Certbot (must be **before** redirect)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect everything else to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}
